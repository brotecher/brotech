# @mojing/capability-matcher

墨境原创的中立能力登记与确定性匹配参考实现。公共出口只包含版本化能力描述、生命周期状态、登记表和基于调用方给定要求的确定性资格判断；它不选择商业供应商、不持有凭据、不作出批准、不执行正式写入，也不访问权威数据。

决策 19 将该制品纳入采用 Apache-2.0 的开放中间层。当前 `private: true` 仅用于阻止未获授权的组织外发布；本地构建和夹具通过不代表已公开发布、真实供应商兼容或生产批准。
