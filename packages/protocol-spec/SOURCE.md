# Source and modifications

- Artifact: `@mojing/protocol-spec@1.0.0`
- Authorized source location: `packages/protocol-spec/` in the current Mojing workspace
- Upstream source: none; Mojing-authored neutral protocol material
- Modification record: M3 initial protocol corpus; split from the internal aggregation package in local commit `c17e255`
- Source acquisition: inspect or archive this package directory from the authorized workspace; no organization-external source URL or publication is authorized
- Generated material: `src/generated/` is reproducibly generated from `protocol-registry.json`, schemas and rules by `tools/protocol/sync-protocol.mjs`

The checked-in `SBOM.spdx.json` records this source snapshot and direct runtime/build dependencies. It is preparation evidence, not a legal opinion or external distribution approval.
