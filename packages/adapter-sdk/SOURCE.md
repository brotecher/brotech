# Source and modifications

- Artifact: `@mojing/adapter-sdk@0.1.0`
- Authorized source location: `packages/adapter-sdk/` in the current Mojing workspace
- Upstream source: none; Mojing-authored neutral Adapter SDK and fixtures
- Modification record: M3 initial SDK and conformance implementation; internal contract dependency removed in local commit `26ec418`
- Source acquisition: inspect or archive this package directory from the authorized workspace; no organization-external source URL or publication is authorized
- Generated material: compiled `dist/` output only; fixed adapters are Mojing-authored test fixtures, not third-party distributions

The checked-in `SBOM.spdx.json` records this source snapshot and direct build dependencies. It is preparation evidence, not a legal opinion or external distribution approval.
